---
'@mds/tokens': minor
'@mds/icons': minor
'@mds/react': minor
---

Initial boilerplate for MDS React design system: three-layer token pipeline (core → modes → semantic) with Style Dictionary, SVG→React icon pipeline with SVGR + optional Figma REST sync, and Button component consuming tokens + ChevronDown icon via the two-layer CSS var indirection convention.
